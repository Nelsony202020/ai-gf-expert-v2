import { guide } from './guide';
import { guideAuthor } from './guideAuthor';
import { productReference, callout, faqSection, guideImage } from './objects';

export const schemaTypes = [guide, guideAuthor, productReference, callout, faqSection, guideImage];
