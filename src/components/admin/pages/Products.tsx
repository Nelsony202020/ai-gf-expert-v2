// Products list page. Individual products are edited in the product
// workspace (src/components/admin/workspace/) — one coherent editor per
// product with Setup/Testing/Verdict/Review/Media/Characters/Pricing/SEO/
// Publish tabs.

import { ProductsListPage } from './ProductsList';

export function ProductsPage() {
  return <ProductsListPage />;
}
