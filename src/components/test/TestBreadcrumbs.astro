---
import Breadcrumbs, { type BreadcrumbItem } from '../ui/Breadcrumbs.astro';

export type TestBreadcrumbItem = BreadcrumbItem;

interface Props {
  items: BreadcrumbItem[];
}

const { items } = Astro.props;
---

<Breadcrumbs items={items} />
