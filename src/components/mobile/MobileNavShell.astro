---
import {
  getMobileNavMode,
  getActiveBottomNavItem,
  showMobileBottomNav,
} from '../../lib/mobile-nav/modes';
import MobileBottomNav from './MobileBottomNav.astro';
import MobileMoreSheet from './MobileMoreSheet.astro';
import DefaultMobileNavSheet from './DefaultMobileNavSheet.astro';

interface Props {
  pathname: string;
}

const { pathname } = Astro.props;
const mode = getMobileNavMode(pathname);
const activeItem = getActiveBottomNavItem(pathname);
const bottomNav = showMobileBottomNav(mode);
---

<script>
  import { bindDrawerScrollFades } from '../../lib/ui/scrollFade';

  bindDrawerScrollFades();
  document.addEventListener('astro:page-load', () => bindDrawerScrollFades());
</script>

{
  bottomNav && (
    <>
      <MobileBottomNav mode={mode} activeItem={activeItem} />
      <MobileMoreSheet />
      <DefaultMobileNavSheet />
    </>
  )
}
